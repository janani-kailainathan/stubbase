---
import { CORE_URL } from '../lib/urls';
import { NAV_GROUPS, RESOURCE_LINKS, LEGAL_LINKS } from '../lib/nav';

const [solutions, features, compare] = NAV_GROUPS;

// Product merges the Solutions and Features spokes into one column.
const productLinks = [...solutions.links, ...features.links];

// "OpenAPI Docs" points at a real, live endpoint: every tenant (including the
// seeded `public` demo) serves GET /<tenant>/openapi.json from the core engine.
const resourceLinks = [
  ...RESOURCE_LINKS,
  { href: `${CORE_URL}/public/openapi.json`, label: 'OpenAPI Docs' },
];

const columns = [
  { heading: 'Product', links: productLinks },
  { heading: 'Compare', links: compare.links },
  { heading: 'Resources', links: resourceLinks },
];
---

<footer class="border-t border-border-c bg-background">
  <div class="mx-auto max-w-7xl px-6 py-14">
    <div class="grid gap-10 md:grid-cols-2 lg:grid-cols-4">
      <div class="lg:col-span-1">
        <img src="/stubbase-logo-text-dark.png" alt="Stubbase" class="mb-4 h-7" data-wordmark="dark" />
        <img
          src="/stubbase-logo-text-light.png"
          alt="Stubbase"
          class="mb-4 h-7"
          data-wordmark="light"
        />
        <p class="max-w-xs text-sm leading-relaxed text-subtle">
          Stubbase is a zero-config, instant backend engine. Generate persistent, relational REST
          APIs via JSON or AI to unblock frontend development and deterministic QA testing.
        </p>
      </div>

      {
        columns.map((column) => (
          <nav aria-label={column.heading}>
            <p class="mb-4 text-xs font-semibold tracking-wide text-subtle uppercase">
              {column.heading}
            </p>
            <ul class="space-y-3">
              {column.links.map((link) => (
                <li>
                  <a
                    href={link.href}
                    class="text-sm text-muted-foreground transition-colors hover:text-foreground"
                  >
                    {link.label}
                  </a>
                </li>
              ))}
            </ul>
          </nav>
        ))
      }
    </div>

    <div
      class="mt-12 flex flex-wrap items-center justify-between gap-4 border-t border-border-c pt-8"
    >
      <p class="text-sm text-subtle">© 2026 Stubbase. stubbase.dev</p>
      <nav class="flex flex-wrap gap-6" aria-label="Legal">
        <a href="/" class="text-sm text-muted-foreground transition-colors hover:text-foreground">Home</a>
        <a href="/pricing" class="text-sm text-muted-foreground transition-colors hover:text-foreground">
          Pricing
        </a>
        {
          LEGAL_LINKS.map((link) => (
            <a href={link.href} class="text-sm text-muted-foreground transition-colors hover:text-foreground">
              {link.label}
            </a>
          ))
        }
      </nav>
    </div>
  </div>
</footer>
