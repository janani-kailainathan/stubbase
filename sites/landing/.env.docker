# Loaded by `bun run build:docker` (astro build --mode docker) — the local
# Caddy stack from docker-compose.
PUBLIC_APP_URL=http://app.stubbase.localhost
PUBLIC_CORE_URL=http://api.stubbase.localhost
