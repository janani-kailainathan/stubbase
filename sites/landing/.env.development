# Loaded only by the Astro dev server (mode=development) — production and
# `build:docker` builds never see it. Ports are owned by scripts/dev.ts.

# "Log in" / "Go to Dashboard" reach the local dashboard dev server.
PUBLIC_APP_URL=http://localhost:5173
# The Home "Try it live" runner fetches <this>/public/users/1 from the local
# core (cross-origin, allowed by the public plane's `Access-Control-Allow-Origin: *`).
PUBLIC_CORE_URL=http://127.0.0.1:3000

# Google One Tap on Home is deliberately NOT pinned here. It comes from the
# repo-root .env (same value as DASHBOARD_GOOGLE_CLIENT_ID), which scripts/dev.ts
# passes through — an empty assignment in this file would override it. Register
# http://localhost:4321 as an authorized JavaScript origin and
# http://localhost:4321/auth/google/one-tap as an authorized redirect URI, or
# Google refuses the prompt.
