-- split into students page,  QA page, mcp storage across multiple llms page, mocking page, then combined landing page

-- mcp, connect from chatgpt or claude and create api

-- seo setup, google analytics and google search console set up

-- split api and pages into two different vms ? research

-- stress test locally

-- deploy to vps

-- go to reddit and search for students groups and start publishing, go startup group and ask for idea approval

-- google and github login to stubbase
   -- enable api created to support register, login, protected api
   -- google and github login

-- since i've rest api only give chat tool to test locally ( run apis )

-- add graphql support

-- payment api support

-- firebase alternative

-- ws support

-- support push notification

-- one click backend

-- login with email verification ( register with email code )

-- subscription support

-- full auth ( with user management, role basee access control )

-- image upload ( s3 upload, other image upload service linking )

-- examples
    - subscription ( ws )
    - google, facebook, login
    - user management


the features are
 - protected and public api
 - one click deployment
 - REST and GrapQL support
 - Google / github / login
 - Websocket support
 - Payment gateway support
 - Email gateway support
 - File upload support via S3 bucket
 - Push notification support
 - Templates to pick from
 - Build and sell your api
 - Debug logs
 - Easy edit and fast deploys
 - Free to start with

I need assets for these topics, how do I get these from canva ?


## bugs and improvements I found

- when I enter 'books.json' it doesn't allow to create
- auth is part of pro feature it should not ( fix messaging )

- on payment related - think of atomicity ( like when it's updated by manually and api ask user which one to use ? )

- when I refresh only the first project is shown, add project to url